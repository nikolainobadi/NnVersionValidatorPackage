import XCTest
@testable import NnVersionValidatorPackage

final class NnVersionValidatorPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NnVersionValidatorPackage().text, "Hello, World!")
    }
}
